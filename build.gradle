plugins {
	id 'java'
}

group = 'com.lz.hexagonal.arch'
version = '0.0.1'
sourceCompatibility = '17'

repositories {
	mavenCentral()
}

subprojects {
	group = 'com.lz.hexagonal.arch'

	apply plugin: 'java'

	repositories {
		mavenCentral()
	}
}